//Math object 
//With window in console you can check what is available for use.
let result;
Math.random(); //Gives an random number between 0 and 1;

result = Math.PI;
result = Math.round(); //Helps to round a number example 2.2  equals 2 
result = Math.ceil(2.1); //Always round up
result = Math.floor(2.2); //Always round down
result = Math.abs(-200); // Returns absolute
result = Math.sqrt(16); //Returns square root of a number
result = Math.min(3,4,1);
result = Math.max(3,4,5,10);
result = Math.floor(Math.random() * 30);
